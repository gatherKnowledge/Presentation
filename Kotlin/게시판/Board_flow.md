### 코틀린 게시판 발표 전략
1. 환경세팅 설명, 어떤 코드를 구현했다.
2. 구현과정에서의 눈에 띠는 것들, 그리고 결과
3. 살펴보지 못한 키워드들, 그리고 적용, 변경 되기 전 과 후의 차이점
4. 느낀점

컨텐츠 목록
이름 - 내용
코틀린의 편한점
이거를 이런식으로

[마리아DB/Tomcat7/JDBC1.1]에서 구동, DB긁어서 Select하는 부분만 구현해 봤습니다.
[테이블 정보]
DB_SEtting  -  어떤 스키마의 데이터베이스 테이블을 쓰는가
[기존 자바 소스] - 서블릿에서 /hello url을 인식하는 서블릿, 마지막에 페이지 포워딩하는 식의 동작

처음에 이제 vo객체를 만들려고하는데, 기존의 자바같은 경우에 만들려고하면 다른 class와 다를 바 없이 정말 무의미한 작업으로 쭉 속성명들 적어주고 앞에 데이터타입 적어주고 getter, setter하는 무의미한 작업이 필요한대
vo_funs  -  kotlin에서 생성한 vo타입인데, 속성 선언하고 아무것도 안해도 바로 getter/setter 사용 가능
vo.png  - 선언한 vo Style 이런식으로 data 키워드를 적어줘서 파일 이름에다가 vo라는 것을 명시하지 않아도 사용 가능하고, 간단하게 어떤 속성들을 사용했는지 불필요한 코드가 없었다. 그리고 컴파일 된 내용이 바로 이내용 인데,
컴파일된obj  -  자바6버저능로 컴파일 된 상태의 코드 기존에 우리가 얘기했듯이 copy,toString그리고 `component메서드`를 사용할 수 있다 그리고 이 부분들을 호출해서 사용하는 부분이 있는데, 바로 json타입 선언 해주듯이 들어갈 수 있다, 자바에서 이 객체를 사용한다고 하면, getter가 제공이 된다.
반대로 자바에서도 사용가능  -  자바에서 Kotlin호출에 대한 아무런 제약이 없음, 여기서 보면 또 getter를 사용
get키워드를 사용하지않음  -  기존에 사용하던 getter/setter는 사용하지 않고, json타입에 들어가듯이, 어디에 들어가서 세팅하는 경우에는 객체.속성 = "값",
jaava에서의setter  - 기존에 사용하던 자바의 setter방식 보여주기용 vs 해서 비교용으로 놔두기

그리고 DB를 사용하려면 Connection객체를 사용해야 하는데, 여기서 문제가 생김. 기존에 클래스의 속성으로 값을 두고, 생성자를 통해서 Conncetion객체를 생성하던 스타일이 통하지 않음, 왜냐하면 코틀린에서는 이게 선언될 때 Null로 두니까 만약 생성자가 실패한다면, select 메소드 실행 할 때 Null값을 가지고 메소드를 실행할수 없다는 논리
코딩스타일변경의필요성  -  기존과 똑같이 갔을 때 생기는 문제점
코딩스타일 변경의 필요성_토론  -  기존에 class에서 갖고가는 속성에 대하여 select 메소드는 connection객체에 대한 null을 확인하지 못하여 사용하지 못함
변경된 코드 스타일 - 기존 자바와 코딩 스타일이 차이가 날 수 밖에 없는 이유 Connection 객체를 파라미터로 전달하는 부분
[토론] 그리고 내가 만든 코딩스타일 얘기해주기

그리고 마지막으로 결과를 select메소드를 실행시켜야하는데 return 타입이 BoardVO이다. 이런경우 자바에서는 변수의 타입을 알려줘야하는데, 혹은 캐스팅을 통해서 변환을 하거나, 코틀린에서는 그렇지 않다. 바로
noCast  -  기존 자바 소스의 경우, boardvo라는 변수한테 object타입을 선언해주고 들어가야하는데 그런것들이 필요 없이 바로 사용가능함 .. boardvo.속성명 이런식


그리고 마지막으로.. jsp 요소인데, 그대로 자바에서 사용하던 Dispacher를 가지고 request객체에 값 넣어주고 jsp던져주니 그대로 실행됨. 정말 100% 호환되는 모습을 볼 수 있음
코틀린개발하면서사용할수있는자바  -  코틀린을 쓴다고하지만 코틀린에서 모든 자바 객체를 사용할 수 있다
툴의 놀라움  -  그래서 기존의 소스를 포팅할 때 역시 문제가 되지 않는 것이,  복사를 했을 때 자바관련 소스라면 이 자바 소스를 코틀린 소스로 변환시킬 것인가라는 것이 나온다
jsp동작 확인  -  기존에 자바 서블릿 동작하듯이 그대로 사용하면 jsp요소들 사용할 수 있음을 알려줌


`여기서 추가해야 할 부분, 키워드에 대한 조사들 추가적.. 그리고 현재 사용하고 있는 소스에서 적용할 때의 유리한 점,
기존 소스에 적용해서 보여주기`

---
내가 하면서 느낀 것들
애초에 Null이 될 수 있는 것들을 명확히 하고 가는 스타일, 그래서 같은 A라는 객체과 A? 객체는 서로 다른 스타일로 취급이 된다.
그리고 내가 하면서 느낀 것들.. 자바에서 하던대로 뚝딱뚝딱 하니까 완성돼서 어안이 벙벙.. 왠만한 코드들은 그냥 쓰던대로 갖다 쓰면 될 듯. 말로만 들었지 기존에 JVM환경에서 돌아가는 라이브러리를 그대로 가져다가 쓰니까 너무 신기, 말로만 하는 것과 실제로 보는 것은 또 다르니까.
제가 생각하는 언어의 본질은, 다른것이 아니라, 얼마나 높은 안정성을 가지고 내가 생각하는 것을 표현해줄 수 있는가? 인데, 확실히 자바는 부족한 언어이구나. 느낌
뜨고 있는 언어들, 자바스크립트, 파이썬, 이미 자바가 할 수 있는 커버리지를 포함하고 있다.

---
전체 플로우
1. 게시판 관련 소개, 세팅 어떤식으로 했다. 환경
- 기존 자바 소스 소개
- 쭉 관련된 봤던 소스들 보여주기
- 사용하지 못 했던 키워드들, 사용할만한 키워드들 적용해서 다시 소스 보여주기
- 정리/느낀 점
---

저는 핵심을 기존의 자바 소스가 어떻게 변환이 되는가에 대해 핵심으로 생각하고 개발 진행을 했습니다.

그리고 마지막 부분, 앞으로 할 것들 목록, 지금까지 생각 해둔 코틀린을 통한 앱 개발 아이디어
